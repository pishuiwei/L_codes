指令：
		ng-app
		ng-init="name:'pwx'";	// 初始化数据
		ng-bind					// 绑定数据大元素的innerHTML
		ng-model 				// 把元素值绑定到应用程序
		ng-repeat 				// 重复元素
		ng-options:				// 设置挑选框 
		ng-if: 					// $even $odd
		ng-show: 				// 显示元素
		ng-hide：				// 隐藏元素
		ng-disabled：			// 直接绑定应用程序数据到HTML的disabled属性
		ng-include:				// 包含指定文件
		
		app.directive:			// 自定义指令