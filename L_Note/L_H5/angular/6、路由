angular路由:
	angular路由允许我们通过不同的URL访问不同的内容，可以实现多视图的单页Web应用
 	1、载入了实现路由的 js 文件：http://apps.bdimg.com/libs/angular-route/1.3.13/angular-route.js。
	2、包含了 ngRoute 模块作为主应用模块的依赖模块。
	3、ng-view
	.when(url,{
	    template: string,		// 如果我们只需要在 ng-view 中插入简单的 HTML 内容，则使用该参数
	    templateUrl: string,	// 如果我们只需要在 ng-view 中插入 HTML 模板文件，则使用该参数
	    controller: string, 	// function、string或数组类型，在当前模板上执行的controller函数，生成新的scope。
	    controllerAs: string,	// string类型，为controller指定别名。
	    redirectTo: string, function,	// 重定向的地址。
	    resolve: object<key, function>	// 指定当前controller所依赖的其他模块
	})
	.otherwise({})
		
	
	var my = angular.module('routingDemoApp',['ngRoute'])
	my.config(['$routeProvider', function($routeProvider){
		$routeProvider
		.when('/',{template:'这是首页页面'})
		.when('/computers',{template:'这是电脑分类页面'})
		.when('/printers',{template:'这是打印机页面'})
		.otherwise({redirectTo:'/'});
	}]);
	    	
	$route    	
	$routeParams  路由参数    	
	$detroy    	  会在页面切换路由时触发，通过destroy我们就可以清理一些不必要的功能，优化页面
	
	$scope.$on("$destroy", function() {  
    });		
    
    
    
    