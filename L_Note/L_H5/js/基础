+严格模式
	"use strict"
+关键字
	typeof 'v1'
	undefined
	delete person.name
	person1 instanceof Object
	name in object
	arguments
+错误与调试
	console对象
	 error(message)：将错误消息记录到控制台
	 info(message)：将信息性消息记录到控制台
	 log(message)：将一般消息记录到控制台
	 warn(message)：将警告消息记录到控制台
+JS中的Boolean
	类型 0 		-0 		""	 	null 	undefined 	NaN 	其他情况
	值  false	false	false	false	false		false	true
+JS逻辑与(&&)	以及逻辑或(||)
	逻辑与:&&操作属于短路操作，只要第一个操作数能够决定结果就不再操作第二个操作数了。
		如果第一个操作数是对象，则返回第二个操作数:"a"&&"b">>b;
		如果第二个操作数是对象，则只有在第一个操作数求值为true的情况下才会返回该对象:false&&"b">>false,true&&"b">>b;
		如果两个都是对象返回第二个:"a"&&"b">>b;
		如果有一个是null,则返回null:null&&"b">>null,"a"&&null>>null;
		如果有一个是NaN,则返回NaN:NaN&&"b">>NaN,"a"&&NaN>>NaN;
		如果有一个操作数为undefined就返回undefined:undefined&&"b">>undefined,"a"&&undefined>>undefined;
		总结:只有两个都为true返回第二个,否则返回false;
	逻辑与:
		如果是第一个是对象就返回第一个:"a"||"b">>a;
		如果第一个求值的结果是false，则返回第二个操作数:false||"b">>b;
		如果两个操作数都是对象，返回第一个:"a"||"b">>a;	
		如果都是null,NaN,undefined,则返回null,NaN,undefined:null||null>>null;
		总结:只要有一个是true就返回true,否则返回false;

		
		
		