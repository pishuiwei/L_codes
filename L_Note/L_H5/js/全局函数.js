+
	eval("alert('hi')"); //把字符串解析为JS代码
	Person.call(o, "Kristen", 25, "Nurse"); //
	person.hasOwnProperty("name"); //判断属性是否在对象中
	
	
	new worker("jsFile")
	requestAnimationFrane()
	cancelRequestAnimationFrame()
