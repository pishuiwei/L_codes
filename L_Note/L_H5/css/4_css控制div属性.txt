(1)设置各个角的圆角、这三个属性效果一样，兼容的浏览器不一样
	border-radius:50px; 
 	border-radius:100px/50px; 前面表示水平半径，后面表示垂直半
	
(2)设置个方向边框的厚度
	border-left: 50px solid(dashed) transparent(透明);
	
(3) box-shadow: h-shadow v-shadow blur spread color inset;
	h-shadow	必需。水平阴影的位置。允许负值。
	v-shadow	必需。垂直阴影的位置。允许负值。
	blur	可选。模糊距离。(第三个值)
	spread	可选。阴影的尺寸	（第4个值）
	color	可选。阴影的颜色。	
	inset	可选。将外部阴影 (outset) 改为内部阴影。

(4)变形
	transform:skew(20deg);
		()
	 	scale：
		缩放，1为原始大小。scale(x)。正数放大，负数缩小。属性值为一个时，x/y轴同时缩放；属性值为两个值时，分别控制x、y轴的缩放。

        rotate：
		水平旋转，属性值格式为Xdeg。(deg是“度”的意思)rotate(Xdeg)。X为正数时，顺时针旋转；为负数时，逆时针旋转

        translate：
		平移，基于XY轴重新定位元素。translate(Xpx,Ypx)。属性值为一个时，x、y轴参数相同；为两个时，x、y轴分别定位

        skew：
		将元素沿水平方向倾斜变形。skew(Xdeg,Ydeg)。这个比较难表述，想象一下平行四边形吧。属性值为一个时，x、y轴参数相同；为两个时，x、y轴各自倾斜

        matrix：矩阵

	()
	none		定义不进行转换。
	scaleX(x)	通过设置 X 轴的值来定义缩放转换。	
	scaleY(y)	通过设置 Y 轴的值来定义缩放转换。	
	scaleZ(z)	通过设置 Z 轴的值来定义 3D 缩放转换。
	translateX(x)	定义转换，只是用 X 轴的值。	
	translateY(y)	定义转换，只是用 Y 轴的值。
	
	scale(x,y)	定义 2D 缩放转换。
	rotate(angle)	定义 2D 旋转，在参数中规定角度。
	translate(x,y)	定义 2D 转换。	
	skew(x-angle,y-angle)	定义沿着 X 和 Y 轴的 2D 倾斜转换。	
	skewX(angle)	定义沿着 X 轴的 2D 倾斜转换。	
	skewY(angle)	定义沿着 Y 轴的 2D 倾斜转换。
	matrix(n,n,n,n,n,n)	定义 2D 转换，使用六个值的矩阵。

	rotate3d(x,y,z,angle)	定义 3D 旋转。	
	rotateX(angle)	定义沿着 X 轴的 3D 旋转。	
	rotateY(angle)	定义沿着 Y 轴的 3D 旋转。	
	rotateZ(angle)	定义沿着 Z 轴的 3D 旋转。
	scale3d(x,y,z)	定义 3D 缩放转换。		
	translate3d(x,y,z)	定义 3D 转换。
	translateZ(z)	定义 3D 转换，只是用 Z 轴的值。			
	perspective(n)	为 3D 转换元素定义透视视图。
	matrix3d(n,n,n,n,n,n,n,n,n,n,n,n,n,n,n,n)定义 3D 转换，使用 16 个值的 4x4矩阵。

(5) 变形的原点（配合变形使用）
	transform-origin: 0 100%;

	0 0；-- 以左上角为原点
	top right；-- 以右上角给原点
	0 100%；-- 以左下角为原点
	100% 100% -- 以右下角

(6)transition : 渐变属性，width在2s之内变为300px
	div{
	width:100px;
	transition: width 2s;
	}
	div:hover{
	width:300px;
	}




