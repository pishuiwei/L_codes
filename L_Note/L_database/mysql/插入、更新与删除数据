+插入数据	
	为表的所有字段插入数据
		insert into table(s1,s2...) values(v1,v2...)
	将查询结果插入到列表中
		insert into table(s1,s2) select v1,v2 from t1
+更新数据
	更新1条数据
		update table set s1 = v1 where (condition)
	批量更新数据
		replace into table(s1,s2...) values(v1,v2...)	
		into table(s1,s2...) values(v1,v2...) on duplicate key update s1=values(s1),s2=values(s2),
		创建临时表,先插入临时表，再更新到实际表
		使用 case when函数加带in的子查询
+删除数据
	delete from table where (condition)