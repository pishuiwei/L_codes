+概述：
            特点： 面向对象、平台可移植、分布式、多线程、安全性
            工作方式： 编译、解释、执行
            开发工具:
                JDK: JAVA、JAVAC、JAVADOC
                IDE：IntlliJ、Eclipse、NetBeans
            语术：JDK、JRE、JVM、API
            源程序结构: package、import、class
            注释: 行‘//’、块‘/*...*/’、文档注释‘/**...*/’
        +数据类型：
            基本数据类型
                整数：byte、short、int、long
                小数：float、double
                char
                boolean
            枚举：enum
            引用类型：String、数组、自定义的类或者接口
+字面量：
            整型字面量：100、100L
            浮点型字面量：3.5、3.5F、3.123e2
            字符字面量：'a'、'\t'、"string"
            布尔型字面量：true、false
            引用字面量：null
            类型字面量：String.class、int.class
+流程控制：
            循环结构：
                分类：while、do...while、for-each、for
                退出：return(结束整个程序)、break（结束循环）、continue（结束本次循环）
            分支结构：if...else、switch...case...default
            顺序结构：正常顺序
+命名规则：
            字母、数字、下滑线、$、数字不能开头
            大小写敏感
            不能跟关键字和保留在冲突(e:null、true、false)
            见名知意
            骆风标识：
                类名每个单词首字母大写
                变量和方法名从第二个单词开始每个单词首字母大写
                命名常量全大写、多个单词用下划线隔开
+标识符：自己给方法、类等定义的名称
+运算符：
            分类：算术运算符(+、-、*、/、%、+=、-=)、赋值运算符(=)、关系运算符(<、<=、>=、>)、
                       短路运算符、条件运算符（三目运算符?:）、
                       自增(++)/自减(--):前面运算在生成值、后面先生成值、在运算
                正负号运算符(+、-)、布尔运算符(&&、||、==、!=)、
                位运算符(&、|、^、~)
                移位运算符(<<、>>、>>>)
                分隔符：{}、[]、()、;、,、.  
            优先级：先乘除在加减
            结合性：从左到右、从右到左(赋值运算、自加自减条件运算、正负号)
     --i 是先执行i=i-1,然后再使用i的值, 这时的i 值就是表达式--i的值。
	 i-- 是先使用i的值作为表达式i--的值,然后,执行i=i-1操作
+代码调试(debug模式):
    F5 进入当前选择的方法
    F6 下一步
    F7 退出当前方法(F5进入的方法)
    F8 继续执行到下个断点,如果没有则结束
    
    Ctrl+Shift+B 添加/去除断点
    Java>Debug>step Filtering 过滤现在的代码
+访问权限控制 ：
        private 自有本类才可以访问
        public 所有地方
        protected 这个类的子类可以访问(不论是否在同一个包)
        (默认) 只能在同有个包中、无论是否是子类          
+关键字：
    private
    public
    default
    final
    static
    instanceof 它返回一个boolean类型的值，意在告诉我们对象是不是某个特定的类型实例
    transient 它声明一个实例变量，当对象存储时，它的值不需要维持。换句话来说就是，用transient关键字标记的成员变量不参与序列化过程。
    assert
    ...